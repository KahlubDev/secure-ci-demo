title = "secure-ci-demo gitleaks configuration"

[extend]
useDefault = true

[[rules]]
id = "demo-api-key"
description = "Detects the intentionally planted demo API key"
regex = '''DEMO_API_KEY=[A-Za-z0-9_-]{20,}'''
keywords = ["DEMO_API_KEY"]